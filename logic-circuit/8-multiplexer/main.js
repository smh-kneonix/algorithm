function multiplexer(inp) {
  const inputLength = inp.length;
  if (inputLength === 1) return `your input length should be bigger than 1`;

 const decimalOutput = parseInt(inp, 2);
 return decimalOutput;

}
multiplexer("010");
